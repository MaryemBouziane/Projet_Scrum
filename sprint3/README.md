# libraries

# permet la recherche tous les noms de chemin à la recherche de fichiers correspondant à un modèle spécifié selon les règles dictées par le shell Unix.

1. pip install glob2

# pour traitement des fichiers xml

2. apt-get install python-lxm

# avoir fonctionnalité de traitement des fichiers pour operating system

3. pip install os_sys


# explication générale du système et la procédure nécessaire pour lancer les programmes.
# pour lancer le programme ,il faut d'abord que les fichiers pdf soient dans le meme emplacement que le parseur.
# Faut installer python et les differnetes librairies citées en dessus 
# en lancant le programme l'utilisateur doit choisir le type de sorte(text ou xml) pour cela faut taper dans notre terminale python3 parser.py -t pour choisir format text ,ou -x pour choisir format xml.
# finalement il vous reste que lancer l'exécution de programme ,pour la genération des fichiers dans la version choisi.



